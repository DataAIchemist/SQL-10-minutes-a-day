-- Selecting customers IDs from Customers table
SELECT cust_id
FROM Customers;

-- Selecting unique products IDs from OrderItems table
SELECT DISTINCT prod_id
FROM OrderItems;

-- Retrieves all columns from Customer table 
SELECT *
FROM Customers;
